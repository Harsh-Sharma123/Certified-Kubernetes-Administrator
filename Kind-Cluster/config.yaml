apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
nodes: 
- role: control-plane
  extraPortMappings:
  - containerPort: 30001   # NodePort
    hostPort: 8080         # Port on your laptop
    protocol: TCP
- role: worker
- role: worker